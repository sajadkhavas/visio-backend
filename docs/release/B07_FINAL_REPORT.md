# B07 — Payment & Reconciliation — Application Report

## Status

B07 application implementation is complete and implementation-head green. Application merge, post-merge verification and the separate docs-only registry transition remain required before B07 can be marked fully closed.

## Coordinates

- B07 START_SHA: `1269be71e7abcde4f01700f7809c416cefcc49e2`
- implementation branch: `phase/visio-b07-payment-reconciliation`
- implementation END_SHA: `9470ccd25bafce793c9b6e6e2f6d3309c8c1772e`
- implementation Gate #250 / run `33978360189`: PASS
- frontend tracker: Issue #55

## Delivered domain

B07 adds durable payment attempts and reconciliation evidence, a provider contract, a concrete fail-closed ZarinPal adapter, authenticated payment creation/detail APIs and an untrusted callback entrypoint that still requires authoritative provider verification.

The implementation keeps payment/provider truth distinct from B06 Order truth. Provider verification is the only source that can produce a verified payment. Redirect/callback status by itself never confirms an Order.

## Money and authority

- commerce-side amount: integer Toman
- provider-side amount: integer IRR
- exact conversion: `IRR = Toman × 10`
- conversion protected by PostgreSQL constraint
- payment request/verify uses the persisted server-authoritative amount
- client totals and frontend payment claims are ignored as truth

## Concurrency and idempotency

B07 enforces:

- unique Order + idempotency key;
- unique provider + non-empty authority;
- one active/verified attempt per Order;
- in-flight `requesting` rejects concurrent duplicate provider requests;
- in-flight `verifying` rejects concurrent duplicate verification;
- duplicate verified callback/reconciliation replays cannot double-consume inventory or reconfirm an Order.

## Financial truth hardening

Provider verification is committed before the B06 Order/inventory transition. If the real payment is verified but Order confirmation cannot safely complete, the payment remains `verified` and B07 records an explicit reconciliation mismatch.

Covered mismatch cases include:

- payment after Order cancellation/expiry;
- reservation/inventory transition failure;
- server-side amount discrepancy.

No mismatch silently rewrites either provider truth or Order truth.

## Provider capability audit

Current official ZarinPal Python SDK was re-checked for:

- v4 request;
- v4 verification;
- StartPay redirect;
- v4 reversal;
- GraphQL refund with access token;
- live and sandbox base URLs.

The adapter implements only those evidenced capabilities. Refund capability is config-gated and B07 exposes no unaudited customer/staff refund authorization endpoint.

## Fail-closed production configuration

Payments remain disabled by default. Enabling production payment requires provider `zarinpal`, a non-empty Merchant ID, an HTTPS callback and non-sandbox mode. Credentials/access tokens remain environment-only and are not committed to Git.

## Migrations

- `apps/payments/migrations/0001_initial.py`
- `apps/payments/migrations/0002_paymentattempt_payments_one_active_attempt_per_order.py`

Both were generated by Django 5.2.17. Migration drift is zero and migrate-from-empty PostgreSQL 16.15 passes.

## Accepted implementation Gate

Backend Quality Gate #250 / run `33978360189` on exact implementation END_SHA `9470ccd25bafce793c9b6e6e2f6d3309c8c1772e`:

- frozen dependency install — PASS
- immutable GitHub Action pins — PASS
- secret sanity — PASS
- Ruff format — PASS
- Ruff lint — PASS
- strict mypy — PASS (`90 source files`)
- Django checks — PASS
- migration drift — 0 / PASS
- migrate from zero PostgreSQL 16.15 — PASS
- PostgreSQL test suite — **91/91 PASS**
- OpenAPI generation/validation — PASS
- production deployment checks — PASS
- Python compilation — PASS

## Closure rule

B07 is not fully closed by this report alone. The application branch must still receive closure documentation, pass an exact closure-head Gate, be frozen, pass the PR exact-head Gate with zero unresolved blocking review threads, merge using the expected head SHA, and pass the post-merge `main` Gate. A separate docs-only registry transition must then record accepted evidence and produce the exact B08 START_SHA.
